---
// Author route → /about/ (Vega is the only author; consolidated content
// lives on /about/ with full bio + credentials + Schema.org Person).
export async function getStaticPaths() {
  return [{ params: { slug: "vega-lin" } }];
}
return Astro.redirect("/about/", 301);
---
